{% extends 'base.html.twig' %}


{% block title %}User{% endblock %}

{% block body %}

    <h2>User</h2>

    {{ form(saveUser) }}

    {% for user in users %}
        <a href="{{ path('adminUserDelete', {'userId' : user.id}) }}">Delete</a>
    {% endfor %}
    <a href="{{ path('entry') }}">Home</a>

{% endblock %}
