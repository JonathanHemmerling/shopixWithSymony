{% extends 'base.html.twig' %}


{% block title %}

{% endblock %}

{% block body %}
    <h3>User Overview</h3>

    <a href="{{ path('entry') }}">Home</a><br/>
    <a href="{{ path('adminCreateUser') }}">Create New User</a>
    <br/>
    <table>
        <tr>
            <th>User:</th>
            <th></th>
        </tr>
        {%for user in allUser %}
        <tr>
            <td>
                <a href="{{ path('adminSaveUserData', { 'userId' : user.id }) }}">{{ user.email }}</a>
            </td>
            <td>
                <a href="{{ path('adminUserDelete', {'userId': user.id}) }}">Delete</a>
            </td>
        </tr>
        {%endfor%}
    </table>
{% endblock %}


